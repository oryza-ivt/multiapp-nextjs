/** @type {import('next').NextConfig} */
// const nextConfig = {};

const nextConfig = {
    basePath: '/transaction',
    assetPrefix: '/transaction',
    output: 'standalone'
  };

export default nextConfig;
