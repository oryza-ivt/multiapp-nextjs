/** @type {import('next').NextConfig} */
// const nextConfig = {};

const nextConfig = {
    basePath: '/transfer',
    assetPrefix: '/transfer',
    output: 'standalone'
  };

export default nextConfig;
